# LabVault 运维检查清单

## 部署前检查

- [ ] 硬件配置符合要求（CPU、内存、硬盘）
- [ ] 操作系统为支持版本（Windows 10+/Ubuntu 20.04+）
- [ ] 网络端口 8000 已开放且无冲突
- [ ] 磁盘空间足够（建议至少 100GB 可用）
- [ ] 备份策略已制定
- [ ] 安全配置已完成（防火墙、访问控制）

## 首次部署

- [ ] 复制 `.env.example` 为 `.env`
- [ ] 修改默认 `admin` 密码
- [ ] 设置强 `SECRET_KEY`
- [ ] 配置 CORS 允许的域名
- [ ] 配置文件存储路径权限
- [ ] 创建数据库初始备份
- [ ] 验证所有功能正常工作

## 每日检查

- [ ] 检查 Hub Server 进程运行状态
- [ ] 检查磁盘空间使用率（< 80%）
- [ ] 查看最近 24 小时错误日志
- [ ] 检查 Leaf Agent 在线状态
- [ ] 统计新上传文件数量
- [ ] 检查数据库连接是否正常

## 每周检查

- [ ] 执行数据库完整性检查
- [ ] 验证备份是否成功执行
- [ ] 审查用户权限分配
- [ ] 检查磁盘 SMART 健康状态
- [ ] 查看异常登录记录
- [ ] 清理临时文件和缓存

## 月度维护

- [ ] 检查依赖包安全更新
- [ ] 归档和清理过期日志
- [ ] 分析性能指标趋势
- [ ] 检查 Leaf Agent 状态文件大小
- [ ] 验证用户账号有效期
- [ ] 执行季度备份恢复演练

## 年度维护

- [ ] 全面安全审计
- [ ] 性能优化评估
- [ ] 硬件升级评估
- [ ] 灾难恢复演练
- [ ] 文档更新

## 应急响应检查

- [ ] 备份文件可用且完整
- [ ] 恢复流程文档齐全
- [ ] 联系方式清单最新
- [ ] 应急环境准备就绪
- [ ] 沟通渠道畅通

## 安全检查

- [ ] 默认密码已修改
- [ ] `SECRET_KEY` 为强密码
- [ ] 文件权限设置正确
- [ ] 定期审查操作日志
- [ ] TLS/HTTPS 已启用（生产环境）
- [ ] 防火墙规则配置正确
- [ ] 定期轮换管理员密码

---

## 快速参考命令

### Hub Server

```bash
# 启动服务
cd hub-server
python main.py

# 检查数据库
# 使用 SQLite 工具查看 labvault.db

# 查看日志（如果配置了日志文件）
tail -f logs/app.log
```

### Leaf Agent

```bash
# 启动监控
cd leaf-agent
python main.py

# 查看状态
cat leaf_state.json
```

### 数据库查询

```sql
-- 查看活跃用户
SELECT * FROM users WHERE status = 'ACTIVE';

-- 查看最近上传
SELECT * FROM files ORDER BY upload_time DESC LIMIT 20;

-- 查看已编辑文件
SELECT * FROM files WHERE edited = 1;

-- 查看操作日志
SELECT * FROM logs ORDER BY timestamp DESC LIMIT 50;
```

---

## 告警阈值

| 指标 | 警告阈值 | 严重阈值 |
|------|----------|----------|
| 磁盘空间 | < 20% | < 10% |
| CPU 使用率 | > 80%（持续 5 分钟） | > 95%（持续 5 分钟） |
| 内存使用率 | > 80% | > 90% |
| 每日新增文件 | > 1000 | > 5000 |
| 错误日志数/小时 | > 10 | > 100 |

---

**最后更新：** 2026-06-02
