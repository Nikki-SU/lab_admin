# LabVault 前端设计修正文档

## 问题点

当前流程：直接在欢迎页选择模式（个人电脑/Hub/Leaf）
修正后流程：先选择设备类型，再选择模式

---

## 正确的使用流程

### 第一阶段：设备选择

用户打开应用时，首先看到设备选择页面：

```
┌─────────────────────────────────────┐
│         🔬 LabVault                 │
│      请选择您要配置的设备类型        │
│                                     │
│  ┌─────────────┐  ┌─────────────┐ │
│  │   🏢 Hub    │  │   🔬 Leaf   │ │
│  │  中央服务器 │  │  实验室电脑 │ │
│  └─────────────┘  └─────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │      💻 PC - 个人电脑          │ │
│  │   登录账号管理和查看数据       │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 第二阶段：模式选择

**选择 Hub 后的流程：**
1. 显示 Hub 配置页面
2. 配置服务器参数
3. 完成后返回设备选择

**选择 Leaf 后的流程：**
1. 显示 Leaf 配置页面
2. 配置实验室设备参数
3. 完成后返回设备选择

**选择 PC 后的流程：**
1. 显示登录页面
2. 登录成功后进入主应用

---

## 设计要点

### 1. 设备选择页面 (DeviceSelection)
- 三种设备类型卡片，优先突出 PC（个人电脑）
- Hub 和 Leaf 作为配置选项
- 每个卡片有清晰的图标和说明文字

### 2. 返回逻辑
- 每个配置页面都有"返回"按钮，回到设备选择页
- PC 登录页面也有"返回"按钮，回到设备选择页

### 3. 状态管理
- 设备选择 → 模式选择 → 功能页面

---

## 文件修改计划

1. **新增组件**：`DeviceSelection.jsx` - 设备选择页面
2. **修改组件**：
   - `WelcomePage.jsx` - 改名为 `DeviceSelection.jsx` 或直接替换
   - `App.tsx` - 更新状态流转逻辑

---

## 状态流转图

```
启动
  ↓
设备选择页面 (DeviceSelection)
  ├─→ Hub → Hub配置
  │       ↓
  │   返回设备选择
  ├─→ Leaf → Leaf配置
  │       ↓
  │   返回设备选择
  └─→ PC → 登录 → 主应用
          ↓
      退出登录
          ↓
      返回设备选择
```
